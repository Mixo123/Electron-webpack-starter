import { Button } from "./lib/components/Buttons";

const App: React.FC = () => {
  return (
    <div>
      <Button href="/" text="Button" size="lg" colors="dark20" />
    </div>
  );
};

export default App;
